# Audio Files Directory

This directory is for audio files that will be served statically by SvelteKit.

- Files placed in the `static` directory are served at the root path of your site
- No processing is applied to these files - they're copied as-is during build
- Large audio files may impact your deployment size
